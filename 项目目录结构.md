# 产品图片管理系统项目目录结构

## 项目概览

产品图片管理系统采用前后端分离架构：
- 前端：React.js + Ant Design
- 后端：Node.js + Express.js
- 数据库：MySQL
- 存储：腾讯云对象存储(COS)

## 目录结构

```
product-images-manager/
│
├── frontend/                      # 前端项目目录
│   ├── public/                    # 静态资源
│   │   ├── index.html            # HTML模板
│   │   ├── favicon.ico           # 网站图标
│   │   └── assets/               # 其他静态资源
│   │
│   ├── src/                       # 源代码
│   │   ├── api/                  # API请求
│   │   │   ├── auth.js           # 认证相关API
│   │   │   ├── images.js         # 图片相关API
│   │   │   ├── tags.js           # 标签相关API
│   │   │   ├── crawler.js        # 爬虫相关API
│   │   │   └── processing.js     # 图片处理相关API
│   │   │
│   │   ├── components/           # 通用组件
│   │   │   ├── common/           # 公共组件
│   │   │   ├── layout/           # 布局组件
│   │   │   ├── images/           # 图片相关组件
│   │   │   ├── tags/             # 标签相关组件
│   │   │   └── processing/       # 处理相关组件
│   │   │
│   │   ├── pages/                # 页面组件
│   │   │   ├── auth/             # 认证页面
│   │   │   │   ├── Login.jsx     # 登录页面
│   │   │   │   └── Register.jsx  # 注册页面
│   │   │   │
│   │   │   ├── dashboard/        # 仪表盘页面
│   │   │   ├── images/           # 图片管理页面
│   │   │   ├── crawler/          # 爬虫管理页面
│   │   │   ├── processing/       # 图片处理页面
│   │   │   ├── tags/             # 标签管理页面
│   │   │   └── settings/         # 设置页面
│   │   │
│   │   ├── hooks/                # 自定义Hooks
│   │   ├── utils/                # 工具函数
│   │   ├── context/              # React上下文
│   │   ├── styles/               # 样式文件
│   │   ├── App.jsx               # 应用入口组件
│   │   ├── index.jsx             # 入口文件
│   │   └── routes.jsx            # 路由配置
│   │
│   ├── package.json              # 依赖配置
│   ├── .env                      # 环境变量
│   ├── .eslintrc.js              # ESLint配置
│   ├── .prettierrc               # Prettier配置
│   └── README.md                 # 前端文档
│
├── backend/                       # 后端项目目录
│   ├── src/                       # 源代码
│   │   ├── api/                  # API路由
│   │   │   ├── auth.js           # 认证路由
│   │   │   ├── images.js         # 图片路由
│   │   │   ├── tags.js           # 标签路由
│   │   │   ├── crawler.js        # 爬虫路由
│   │   │   └── processing.js     # 处理路由
│   │   │
│   │   ├── controllers/          # 控制器
│   │   │   ├── authController.js # 认证控制器
│   │   │   ├── imageController.js # 图片控制器
│   │   │   ├── tagController.js  # 标签控制器
│   │   │   ├── crawlerController.js # 爬虫控制器
│   │   │   └── processingController.js # 处理控制器
│   │   │
│   │   ├── models/               # 数据模型
│   │   │   ├── User.js           # 用户模型
│   │   │   ├── Image.js          # 图片模型
│   │   │   ├── Tag.js            # 标签模型
│   │   │   ├── TagCategory.js    # 标签分类模型
│   │   │   ├── ImageTag.js       # 图片标签关联模型
│   │   │   ├── ProcessingTask.js # 处理任务模型
│   │   │   └── CrawlerTask.js    # 爬虫任务模型
│   │   │
│   │   ├── services/             # 业务逻辑
│   │   │   ├── authService.js    # 认证服务
│   │   │   ├── imageService.js   # 图片服务
│   │   │   ├── tagService.js     # 标签服务
│   │   │   ├── crawlerService.js # 爬虫服务
│   │   │   └── processingService.js # 处理服务
│   │   │
│   │   ├── utils/                # 工具函数
│   │   │   ├── logger.js         # 日志工具
│   │   │   ├── validator.js      # 验证工具
│   │   │   ├── cosUploader.js    # COS上传工具
│   │   │   └── imageProcessor.js # 图片处理工具
│   │   │
│   │   ├── middlewares/          # 中间件
│   │   │   ├── auth.js           # 认证中间件
│   │   │   ├── errorHandler.js   # 错误处理中间件
│   │   │   └── validator.js      # 请求验证中间件
│   │   │
│   │   ├── config/               # 配置文件
│   │   │   ├── database.js       # 数据库配置
│   │   │   ├── cos.js            # COS配置
│   │   │   └── app.js            # 应用配置
│   │   │
│   │   ├── crawlers/             # 爬虫模块
│   │   │   ├── googleCrawler.js  # 谷歌爬虫
│   │   │   ├── amazonCrawler.js  # 亚马逊爬虫
│   │   │   ├── bingCrawler.js    # 必应爬虫
│   │   │   └── 1688Crawler.js    # 1688爬虫
│   │   │
│   │   ├── processors/           # 图片处理模块
│   │   │   ├── backgroundRemover.js # 背景移除
│   │   │   ├── resizer.js        # 尺寸调整
│   │   │   ├── borderAdjuster.js # 边界调整
│   │   │   └── textRemover.js    # 文字移除
│   │   │
│   │   ├── ai/                   # AI模块
│   │   │   ├── imageGenerator.js # 图片生成
│   │   │   └── tagRecommender.js # 标签推荐
│   │   │
│   │   ├── db/                   # 数据库相关
│   │   │   ├── connection.js     # 数据库连接
│   │   │   ├── migrations/       # 数据库迁移
│   │   │   └── seeders/          # 数据库种子
│   │   │
│   │   ├── app.js                # 应用入口
│   │   └── server.js             # 服务器启动
│   │
│   ├── tests/                     # 测试文件
│   │   ├── unit/                 # 单元测试
│   │   ├── integration/          # 集成测试
│   │   └── fixtures/             # 测试数据
│   │
│   ├── scripts/                   # 脚本文件
│   │   ├── setup.js              # 初始化脚本
│   │   └── seed.js               # 数据填充脚本
│   │
│   ├── logs/                      # 日志文件
│   ├── uploads/                   # 临时上传文件
│   ├── package.json               # 依赖配置
│   ├── .env                       # 环境变量
│   ├── .eslintrc.js               # ESLint配置
│   ├── .prettierrc                # Prettier配置
│   └── README.md                  # 后端文档
│
├── docs/                          # 项目文档
│   ├── api/                       # API文档
│   ├── database/                  # 数据库文档
│   ├── deployment/                # 部署文档
│   └── user-guide/                # 用户指南
│
├── scripts/                       # 项目脚本
│   ├── setup.sh                   # 环境设置脚本
│   └── deploy.sh                  # 部署脚本
│
├── .gitignore                     # Git忽略文件
├── .editorconfig                  # 编辑器配置
├── docker-compose.yml             # Docker配置
├── README.md                      # 项目说明
└── LICENSE                        # 许可证文件
```

## 关键文件说明

### 前端关键文件

1. **src/api/**: 包含所有与后端API通信的函数
2. **src/components/**: 可复用的UI组件
3. **src/pages/**: 页面级组件，对应路由
4. **src/context/**: React上下文，用于状态管理
5. **src/utils/**: 工具函数，如格式化、验证等

### 后端关键文件

1. **src/api/**: 定义API路由
2. **src/controllers/**: 处理请求和响应
3. **src/models/**: 数据模型定义
4. **src/services/**: 业务逻辑实现
5. **src/middlewares/**: 中间件，如认证、错误处理
6. **src/crawlers/**: 爬虫实现
7. **src/processors/**: 图片处理实现
8. **src/ai/**: AI相关功能实现

## 技术栈详情

### 前端技术栈

- **React.js**: UI库
- **React Router**: 路由管理
- **Ant Design**: UI组件库
- **Axios**: HTTP客户端
- **React Query**: 数据获取和缓存
- **Zustand/Context API**: 状态管理
- **React Hook Form**: 表单处理
- **Fabric.js/Konva**: 图片编辑
- **Day.js**: 日期处理

### 后端技术栈

- **Node.js**: JavaScript运行时
- **Express.js**: Web框架
- **Sequelize**: ORM工具
- **MySQL**: 数据库
- **Multer**: 文件上传处理
- **Sharp**: 图片处理
- **Puppeteer/Cheerio**: 网页爬虫
- **JWT**: 认证
- **Winston**: 日志记录
- **Joi**: 数据验证
- **Cos-nodejs-sdk-v5**: 腾讯云对象存储SDK

## 开发环境设置

1. 克隆仓库
2. 安装前端依赖：
   ```
   cd frontend
   npm install
   ```
3. 安装后端依赖：
   ```
   cd backend
   npm install
   ```
4. 配置环境变量：
   - 复制`.env.example`为`.env`并填写配置
5. 初始化数据库：
   ```
   cd backend
   npm run db:migrate
   npm run db:seed
   ```
6. 启动开发服务器：
   - 前端：`cd frontend && npm start`
   - 后端：`cd backend && npm run dev`

## 部署流程

1. 构建前端：
   ```
   cd frontend
   npm run build
   ```
2. 构建后端：
   ```
   cd backend
   npm run build
   ```
3. 使用Docker部署：
   ```
   docker-compose up -d
   ```

## 扩展性考虑

1. **模块化设计**: 每个功能都是独立模块，便于扩展
2. **插件系统**: 为爬虫和处理器提供插件接口
3. **API版本控制**: 支持API版本演进
4. **微服务准备**: 结构设计便于未来拆分为微服务